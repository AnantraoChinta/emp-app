{
  "name": "emp-app",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -r \"npm:start-server\" \"npm:start-client\"",
    "start-server": "npm --prefix server run dev",
    "start-client": "npm --prefix client run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}

